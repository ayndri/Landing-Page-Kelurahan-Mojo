; Menaikkan batas unggah file (foto tanaman/UMKM/galeri).
; Berlaku untuk hosting PHP-CGI/FPM (umum di shared hosting/cPanel).
; Jika server memakai Apache mod_php, atur nilai yang sama di php.ini
; atau public/.htaccess (php_value upload_max_filesize 20M).
upload_max_filesize = 20M
post_max_size = 25M
max_file_uploads = 20
